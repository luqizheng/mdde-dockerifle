
services:
  dotnet-dev:
    image: zhcoder-docker-registry.com:8000/dev-env/nodejs:v16
    container_name: ${container_name:-node_v16}
    volumes:
      - ${workspace}:/workspace
      - nuget-packages:/home/developer/.nuget/packages
    working_dir: /workspace
    ports:
      - "${APP_PORT:-5001:5660}"
    command: tail -f /dev/null
    networks:
      - dev-network

volumes:
  nuget-packages:

networks:
  dev-network:
    driver: bridge
